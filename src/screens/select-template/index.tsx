import {ScrollView, Text} from 'react-native';
import {SafeAreaView} from 'react-native-safe-area-context';

const SelectTemplateScreen = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>SelectTemplateScreen</Text>
      </ScrollView>
    </SafeAreaView>
  );
};
export default SelectTemplateScreen;
